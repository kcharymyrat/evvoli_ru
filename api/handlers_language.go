package api

import (
	"net/http"
)

func (app *ApiApplication) CreateLanguageHandler(w http.ResponseWriter, r *http.Request) {
	// var langDTO LanguageRequestDTO

	// Get the langDTO from the request body

	// Validate langDTO

	// Create DB instance of language table

	// Respond with 201 - created or error
}

func (app *ApiApplication) GetAllLanguagesHandler(w http.ResponseWriter, r *http.Request) {
}
